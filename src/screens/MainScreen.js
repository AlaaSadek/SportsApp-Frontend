import React from "react";
import {
  Text,
} from "react-native";

const MainScreen = ({ navigation }) => {
    return (
      
       <Text>
           Main Screen
       </Text>
    );
  };

  export default MainScreen;